---
phase: 03-agent-ux-and-mcp-tool-visibility
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/agents.py
  - frontend/lib/api.ts
  - frontend/components/AgentForm.tsx
  - frontend/app/agents/page.tsx
  - frontend/app/agents/new/page.tsx
  - frontend/app/agents/[id]/page.tsx
  - frontend/app/agents/[id]/edit/page.tsx
autonomous: true

must_haves:
  truths:
    - "Agent list shows last execution status, time, and token count for each agent"
    - "Agent creation form includes all fields: name, description, model, system_prompt, temperature, skills multi-select, integrations multi-select, schedule, memory_enabled"
    - "Agent edit form loads current values including skill/integration assignments"
    - "User can trigger agent run from detail page and see execution result with final output, steps, and error details"
  artifacts:
    - path: "backend/app/api/agents.py"
      provides: "include_last_execution query param on GET /api/agents"
      contains: "include_last_execution"
    - path: "frontend/components/AgentForm.tsx"
      provides: "Reusable agent form with React Hook Form + Zod validation and skill/integration multi-select"
      min_lines: 100
    - path: "frontend/app/agents/page.tsx"
      provides: "Enhanced agent list with last execution summary per card"
      contains: "last_execution"
    - path: "frontend/app/agents/[id]/page.tsx"
      provides: "Agent detail with execution result display and run trigger"
      contains: "ExecutionResultView"
    - path: "frontend/app/agents/[id]/edit/page.tsx"
      provides: "Agent editing page using AgentForm component"
      min_lines: 30
    - path: "frontend/app/agents/new/page.tsx"
      provides: "Agent creation page using AgentForm component"
      contains: "AgentForm"
  key_links:
    - from: "frontend/app/agents/page.tsx"
      to: "/api/agents?include_last_execution=true"
      via: "agentsAPI.list call with include param"
      pattern: "include_last_execution"
    - from: "frontend/components/AgentForm.tsx"
      to: "frontend/lib/api.ts"
      via: "skillsAPI.list and integrationsAPI.list for multi-select options"
      pattern: "skillsAPI\\.list|integrationsAPI\\.list"
    - from: "frontend/app/agents/[id]/page.tsx"
      to: "/api/executions/{id}"
      via: "executionsAPI.get for execution result display"
      pattern: "executionsAPI\\.get"
---

<objective>
Complete the agent UX workflows: enhanced list with execution context, full create/edit forms with skill and integration assignment, manual run trigger with execution result display.

Purpose: Satisfies CTRL-01 (agent list with status and recent run context), CTRL-02 (create/update agents with full configuration), and CTRL-03 (manually trigger and view execution results). This is the core agent management experience.

Output: Enhanced agent list page, reusable AgentForm component, agent edit page, execution result view in agent detail, minor backend enhancement for include_last_execution.
</objective>

<execution_context>
@/home/kngpnn/.claude/get-shit-done/workflows/execute-plan.md
@/home/kngpnn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-ux-and-mcp-tool-visibility/03-RESEARCH.md
@frontend/lib/api.ts
@frontend/app/agents/page.tsx
@frontend/app/agents/new/page.tsx
@frontend/app/agents/[id]/page.tsx
@backend/app/api/agents.py
@backend/app/api/executions.py
@backend/app/models/agent.py
@backend/app/models/execution.py
@frontend/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend include_last_execution endpoint + install frontend form deps + create AgentForm component</name>
  <files>
    backend/app/api/agents.py
    frontend/lib/api.ts
    frontend/components/AgentForm.tsx
  </files>
  <action>
IMPORTANT: Use the /frontend-design skill for all UI component work in this task to ensure high-quality design.

**Backend (backend/app/api/agents.py):**

Add an optional `include_last_execution: bool = False` query parameter to the `list_agents` endpoint. When true, for each agent, query the most recent Execution (ordered by `created_at DESC`, limit 1) where `Execution.agent_id == agent.id`. Return agents as a list of dicts (not raw ORM objects) where each dict includes all existing AgentResponse fields plus an optional `last_execution` object containing: `{id, status, trigger, started_at, completed_at, tokens_input, tokens_output, error_message}`. Use a single SQL query with a lateral subquery or a post-fetch loop (the agent count is small enough that a loop is acceptable). When `include_last_execution` is false (default), return the existing AgentResponse list unchanged for backward compatibility.

Add the necessary imports: `from app.models.execution import Execution` and `from sqlalchemy import desc`. Build the response dicts manually when `include_last_execution=True` to include the extra field outside the Pydantic model.

**Frontend deps:**

Run `npm install react-hook-form zod @hookform/resolvers` in the frontend directory. Do NOT install react-json-tree (we use a custom SchemaRenderer instead).

**Frontend API types (frontend/lib/api.ts):**

Add a new `ExecutionSummaryBrief` interface:
```typescript
export interface ExecutionSummaryBrief {
  id: string;
  status: string;
  trigger: string;
  started_at: string | null;
  completed_at: string | null;
  tokens_input: number;
  tokens_output: number;
  error_message: string | null;
}
```

Add a new type: `export type AgentWithExecution = Agent & { last_execution?: ExecutionSummaryBrief }`.

Update `agentsAPI.list` to accept an optional `include_last_execution` boolean parameter:
```typescript
list: (skip = 0, limit = 100, include_last_execution?: boolean) =>
  fetchAPI<AgentWithExecution[]>('/api/agents', {
    params: { skip, limit, include_last_execution }
  }),
```

Also update `agentsAPI.create` to accept the full `AgentCreate` shape (currently limited to name/description/model/system_prompt). Expand it to:
```typescript
create: (data: {
  name: string;
  description?: string;
  model?: string;
  system_prompt?: string;
  temperature?: number;
  skills?: string[];
  integrations?: string[];
  schedule?: string;
  memory_enabled?: boolean;
}) => fetchAPI<Agent>('/api/agents', { method: 'POST', body: JSON.stringify(data) }),
```

**AgentForm component (frontend/components/AgentForm.tsx):**

Create a reusable "use client" form component using React Hook Form + Zod. This component is used by both create and edit pages.

Props: `{ initialData?: Partial<AgentFormData>; onSubmit: (data: AgentFormData) => Promise<void>; submitLabel?: string }`.

Zod schema `agentFormSchema`:
- name: z.string().min(1, "Name is required").max(255)
- description: z.string().optional().or(z.literal(""))
- model: z.string().min(1, "Model is required")
- system_prompt: z.string().optional().or(z.literal(""))
- temperature: z.coerce.number().min(0).max(2)
- skills: z.array(z.string())
- integrations: z.array(z.string())
- schedule: z.string().optional().or(z.literal(""))
- memory_enabled: z.boolean()

Form layout (use existing design system classes -- .card, .input, .input-group, .input-label, .btn, .btn-primary, CSS variables):
1. **Basic Info section**: Name (text input, required), Description (textarea)
2. **Model Configuration section**: Model (select dropdown with same MODELS array as new agent page: gpt-4o-mini, gpt-4o, claude-3-5-sonnet, claude-3-5-haiku), Temperature (number input with 0.1 step), System Prompt (textarea, 4 rows min)
3. **Skills section**: Fetch available skills via `skillsAPI.list()` on mount. Render as checkbox list. Each checkbox shows skill name and description. Use Controller from react-hook-form for the array field. Show category badge next to each skill.
4. **Integrations section**: Fetch available integrations via `integrationsAPI.list()` on mount. Render as checkbox list. Each checkbox shows integration name, type badge, and status indicator.
5. **Advanced section**: Schedule (text input with cron examples in helper text), Memory Enabled (checkbox toggle)
6. Submit button using submitLabel prop (defaults to "Save Agent")

Use `form.reset(initialData)` pattern for edit mode as described in 03-RESEARCH.md pitfall 1. Show inline validation errors under each field. Show form-level error banner if submission fails. Disable submit while `isSubmitting`.

Style the form sections with clear visual separation using cards or bordered sections within the form. Use the dark theme CSS variables (--color-bg-tertiary for section backgrounds, --color-border, --color-text-secondary for labels).
  </action>
  <verify>
    1. `cd /home/kngpnn/dev/lazy-agents/backend && python -c "from app.api.agents import router; print('Backend OK')"` succeeds
    2. `cd /home/kngpnn/dev/lazy-agents/frontend && npx tsc --noEmit` compiles without errors
    3. `curl http://localhost:8000/api/agents?include_last_execution=true` returns agent list (when backend running)
    4. AgentForm.tsx exists and exports AgentForm component
  </verify>
  <done>
    Backend list_agents endpoint accepts include_last_execution=true and returns last_execution data per agent. Frontend API client types updated with AgentWithExecution and expanded create signature. AgentForm component exists with React Hook Form + Zod, multi-select for skills/integrations, all agent fields, inline validation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance agent list, create, edit, and detail pages with execution results</name>
  <files>
    frontend/app/agents/page.tsx
    frontend/app/agents/new/page.tsx
    frontend/app/agents/[id]/page.tsx
    frontend/app/agents/[id]/edit/page.tsx
  </files>
  <action>
IMPORTANT: Use the /frontend-design skill for all UI work in this task to ensure high-quality design that matches the existing dark-mode design system.

**Agent List Page (frontend/app/agents/page.tsx):**

Enhance the existing agent list to satisfy CTRL-01. Changes:
1. Call `agentsAPI.list(0, 100, true)` to include last execution data.
2. Update the state type from `Agent[]` to `AgentWithExecution[]` (import from api.ts).
3. In each agent card, below the existing runs/success stats row, add a "Last Run" summary section:
   - Show only when `agent.last_execution` exists
   - Display a compact row with: status badge (using existing badge classes: badge-success for success, badge-error for failed, badge-info for running, badge-neutral for pending/cancelled), relative time (e.g., "2 min ago", "3h ago" -- write a simple `formatRelativeTime` helper), and total tokens used
   - If the last execution failed and has error_message, show a truncated (1-line, ellipsis) error preview in text-error color
4. Keep all existing functionality (delete, run, toggle status, create link) intact.
5. Add an "Edit" button alongside the existing "Configure" link, or change "Configure" to link to the edit page `/agents/${agent.id}/edit`.

**Agent Create Page (frontend/app/agents/new/page.tsx):**

Replace the existing two-step describe/configure flow with a cleaner single-page form using the AgentForm component:
1. Import AgentForm from `@/components/AgentForm`
2. Wrap in DashboardLayout with header "Create Agent"
3. Render `<AgentForm onSubmit={handleCreate} submitLabel="Create Agent" />`
4. handleCreate calls `agentsAPI.create(data)` then `router.push(/agents/${agent.id})`
5. Show error banner on failure
6. Keep it simple -- no more "describe first" wizard. The AgentForm handles all fields.

**Agent Edit Page (frontend/app/agents/[id]/edit/page.tsx):**

Create new page:
1. "use client" component wrapped in DashboardLayout
2. Fetch agent config via `agentsAPI.getConfig(id)` on mount
3. Transform loaded data to match AgentForm's expected shape: map `agent.skills` (which are skill detail objects from getConfig) back to string IDs array, same for integrations
4. Render `<AgentForm initialData={transformedData} onSubmit={handleUpdate} submitLabel="Update Agent" />`
5. handleUpdate calls `agentsAPI.update(id, data)` then `router.push(/agents/${id})`
6. Show loading state while fetching, error state if fetch fails
7. Include "Back to Agent" link in header

**Agent Detail Page (frontend/app/agents/[id]/page.tsx):**

Enhance to satisfy CTRL-03. Changes:
1. Add an "Edit" button in the header actions (links to `/agents/${agent.id}/edit`)
2. Enhance the "history" tab: when a user clicks on an execution row in the table, expand inline or navigate to show execution details. Implement as an expandable row approach:
   - Clicking a row sets `selectedExecutionId` state
   - When selected, fetch full execution via `executionsAPI.get(executionId)`
   - Show expanded detail below the row with:
     a. Output data section: if `execution.output_data` exists, display as formatted JSON in a `<pre>` block with --color-bg-tertiary background
     b. Steps section: list each step with step_number, name, step_type badge, status badge, and if expanded further, input/output data
     c. Error section: if `execution.error_message`, show in a card with --color-error-muted background
     d. Metadata: started_at, completed_at, duration, tokens, cost
   - Click again to collapse
3. After handleRun succeeds, switch to the history tab and call loadExecutions() to show the new execution. Add a brief auto-refresh: set a 3-second interval that re-fetches executions while the latest execution status is "pending" or "running", clear interval when done.
4. Keep all existing tabs (overview, skills, schedule, logs, history) intact and functional.
  </action>
  <verify>
    1. `cd /home/kngpnn/dev/lazy-agents/frontend && npx tsc --noEmit` passes
    2. `cd /home/kngpnn/dev/lazy-agents/frontend && npm run build` succeeds
    3. Agent list page loads and shows last execution data (when backend has execution data)
    4. `/agents/new` renders the AgentForm with all fields
    5. `/agents/[id]/edit` renders AgentForm pre-filled with agent data
    6. `/agents/[id]` history tab allows clicking an execution to see details
  </verify>
  <done>
    Agent list page shows last execution summary per agent card. Agent create page uses AgentForm with all fields including skill/integration multi-select. Agent edit page loads current agent data into AgentForm. Agent detail page has execution result view in history tab with output, steps, and errors. User can trigger a run and see its result update in the history tab.
  </done>
</task>

</tasks>

<verification>
1. `cd /home/kngpnn/dev/lazy-agents/frontend && npx tsc --noEmit` -- TypeScript compiles cleanly
2. `cd /home/kngpnn/dev/lazy-agents/frontend && npm run build` -- Next.js production build succeeds
3. `cd /home/kngpnn/dev/lazy-agents/backend && python -m pytest backend/tests/ -x` -- Backend tests pass (existing + any new)
4. Agent list page at `/agents` renders cards with last execution summaries
5. Create agent form at `/agents/new` shows all fields with validation
6. Edit agent form at `/agents/[id]/edit` loads and pre-fills current data
7. Agent detail execution history at `/agents/[id]` shows expandable execution results
</verification>

<success_criteria>
- CTRL-01: Agent list cards show status, last run time, execution result summary, and token usage
- CTRL-02: Agent create/edit forms include all fields (name, description, model, system_prompt, temperature, skills, integrations, schedule, memory_enabled) with Zod validation
- CTRL-03: User can trigger run from agent detail and view completion status, final output, execution steps, and error details
- All existing agent functionality (delete, toggle status, schedule, logs) remains intact
- TypeScript compiles and Next.js builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-ux-and-mcp-tool-visibility/03-01-SUMMARY.md`
</output>
