---
phase: 03-agent-ux-and-mcp-tool-visibility
plan: "03"
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Frontend TypeScript compiles with zero errors after all phase 3 changes"
    - "Frontend production build succeeds after all phase 3 changes"
    - "Backend tests pass after agents endpoint enhancement"
    - "All new pages render without runtime errors"
  artifacts: []
  key_links:
    - from: "frontend/lib/api.ts"
      to: "backend/app/api/agents.py"
      via: "AgentWithExecution type matches backend include_last_execution response shape"
      pattern: "last_execution"
    - from: "frontend/components/AgentForm.tsx"
      to: "backend/app/api/agents.py"
      via: "Form fields match AgentCreate/AgentUpdate Pydantic schema fields"
      pattern: "name.*description.*model.*system_prompt.*temperature.*skills.*integrations"
---

<objective>
Validate that all phase 3 frontend/backend contracts are sound: TypeScript compiles, production build succeeds, backend tests pass, and new pages render correctly.

Purpose: Catches contract drift between frontend API client types and backend response shapes, verifies no regressions from phase 3 changes, and provides human verification of the final UX quality.

Output: Validated build artifacts, human-verified UX quality.
</objective>

<execution_context>
@/home/kngpnn/.claude/get-shit-done/workflows/execute-plan.md
@/home/kngpnn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-ux-and-mcp-tool-visibility/03-01-SUMMARY.md
@.planning/phases/03-agent-ux-and-mcp-tool-visibility/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full build validation and fix any contract issues</name>
  <files></files>
  <action>
Run the following validation steps sequentially. If any step fails, diagnose and fix the issue before proceeding.

**Step 1: Frontend TypeScript check**
```
cd /home/kngpnn/dev/lazy-agents/frontend && npx tsc --noEmit
```
If errors: fix type mismatches in the files from 03-01 and 03-02. Common issues:
- AgentWithExecution type not matching actual backend response
- SchemaRenderer prop types too strict for Record<string, unknown> data
- Missing imports or incorrect path aliases

**Step 2: Frontend ESLint**
```
cd /home/kngpnn/dev/lazy-agents/frontend && npm run lint
```
If errors: fix lint violations. Common issues:
- Unused imports from refactored code
- Missing React Hook Form dependency in useEffect arrays
- any types that should be typed

**Step 3: Frontend production build**
```
cd /home/kngpnn/dev/lazy-agents/frontend && npm run build
```
If errors: fix build failures. This catches runtime import issues, missing modules, and SSR problems that tsc alone misses.

**Step 4: Backend lint check**
```
cd /home/kngpnn/dev/lazy-agents/backend && python -m ruff check app/api/agents.py
```
If errors: fix ruff violations in the modified backend file.

**Step 5: Backend tests**
```
cd /home/kngpnn/dev/lazy-agents/backend && python -m pytest tests/ -x --tb=short
```
If failures: fix test issues. The include_last_execution enhancement should not break existing agent tests.

**Step 6: Contract spot-check**
Verify that the frontend API types match backend response shapes by reading both files and comparing:
- `AgentWithExecution.last_execution` fields match what `list_agents` returns when `include_last_execution=true`
- `AgentForm` field names match `AgentCreate` Pydantic model field names
- `MCPServer.tools_detected` shape matches what SchemaRenderer expects
- `ExecutionResponse.steps` shape matches what the execution detail view renders

If any mismatch found, fix the frontend type or adjust the backend response to align.
  </action>
  <verify>
    1. `npx tsc --noEmit` exits 0
    2. `npm run lint` exits 0
    3. `npm run build` exits 0
    4. `python -m ruff check app/api/agents.py` exits 0
    5. `python -m pytest tests/ -x` exits 0 with no failures
  </verify>
  <done>
    All frontend and backend validation checks pass. Type contracts between frontend API client and backend responses are verified as aligned. No regressions in existing tests.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete agent UX and MCP tool browser for Phase 3:

    1. **Agent List** (`/agents`): Cards now show last execution status, time, and token usage
    2. **Agent Create** (`/agents/new`): Full form with name, description, model, system prompt, temperature, skills multi-select, integrations multi-select, schedule, memory toggle -- all with Zod validation
    3. **Agent Edit** (`/agents/{id}/edit`): Same form pre-filled with current agent configuration
    4. **Agent Detail** (`/agents/{id}`): History tab has expandable execution rows showing output data, execution steps, and error details. Run button triggers execution and auto-refreshes results.
    5. **MCP Tool Browser** (`/integrations/mcp/{id}/tools`): Shows all discovered tools with expandable input schema details (parameter names, types, required indicators, descriptions, enums, defaults)
    6. **MCP Server Table** (`/integrations/mcp`): Tools column is clickable, linking to tool browser. "Tools" action button added.
  </what-built>
  <how-to-verify>
    Start the dev stack: `docker-compose up` or run backend + frontend separately.

    **CTRL-01 -- Agent list with execution context:**
    1. Navigate to `/agents`
    2. Verify agent cards show status badge, model, run count, success rate
    3. If any agent has been run, verify "Last Run" section shows status badge, relative time, and token count
    4. Verify layout is clean and matches dark theme

    **CTRL-02 -- Agent create/edit:**
    5. Click "Create Agent" -- verify full form renders with all fields
    6. Try submitting with empty name -- verify validation error appears inline
    7. Fill all fields, select skills/integrations if available, submit -- verify agent is created
    8. Navigate to the new agent, click "Edit" -- verify form loads with all saved values
    9. Change a field and save -- verify update persists

    **CTRL-03 -- Agent run and results:**
    10. On agent detail page, click "Run Now"
    11. Verify the history tab shows the new execution (may need to wait a few seconds)
    12. Click the execution row -- verify it expands to show output data, steps, and metadata
    13. If execution failed, verify error message is displayed clearly

    **MCP-05 -- Tool browser:**
    14. Navigate to `/integrations/mcp`
    15. If MCP servers exist with tools, click the tool count or "Tools" button
    16. Verify tool browser page shows all discovered tools with names and descriptions
    17. Click "Expand Schema" on a tool -- verify input parameters are displayed with types, required badges, and descriptions
    18. Verify "Back to MCP Servers" link works

    **Regression:**
    19. Verify existing agent delete, toggle status, and schedule features still work
    20. Verify existing MCP server create/edit/delete/restart/sync still work
  </how-to-verify>
  <resume-signal>Type "approved" to complete phase 3, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. All automated checks pass (tsc, lint, build, pytest)
2. Human has verified all four requirements (CTRL-01, CTRL-02, CTRL-03, MCP-05) in the running application
3. No regressions in existing functionality
</verification>

<success_criteria>
- Zero TypeScript errors, zero lint errors, successful production build
- Backend tests pass with no regressions
- Human confirms: agent list shows execution context, agent forms work with full fields, execution results are viewable, MCP tool browser displays schemas correctly
- All existing functionality (agent CRUD, MCP server management) remains intact
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-ux-and-mcp-tool-visibility/03-03-SUMMARY.md`
</output>
