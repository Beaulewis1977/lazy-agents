---
phase: 01-security-and-deployment-baseline
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - backend/app/core/security.py
  - backend/app/runtime/agent_executor.py
  - backend/app/api/websocket.py
  - backend/app/api/agents.py
  - backend/tests/test_secret_redaction.py
  - frontend/app/settings/page.tsx
autonomous: true
must_haves:
  truths:
    - "Credential values are never returned in API responses intended for operator UI."
    - "Execution/system logs and persisted execution payloads do not expose plaintext credential values."
    - "Credential entry fields remain masked and are not persisted in browser localStorage as canonical storage."
  artifacts:
    - path: backend/app/core/security.py
      provides: "Central redaction/masking helpers for sensitive keys and token-like values"
      min_lines: 50
    - path: backend/app/runtime/agent_executor.py
      provides: "Redaction applied before execution persistence/log emission"
      min_lines: 60
    - path: frontend/app/settings/page.tsx
      provides: "UI handling that avoids persisting plaintext provider/API keys"
      min_lines: 40
  key_links:
    - from: backend/app/core/security.py
      to: backend/app/runtime/agent_executor.py
      via: "redaction helper call before saving execution input/output and emitting logs"
      pattern: "redact|mask"
    - from: backend/app/core/security.py
      to: backend/app/api/websocket.py
      via: "log message sanitization before broadcast"
      pattern: "redact|emit"
    - from: frontend/app/settings/page.tsx
      to: backend/app/api/agents.py
      via: "masked/transient credential UI behavior without localStorage secret persistence"
      pattern: "localStorage|password"
---

<objective>
Standardize secret masking/redaction across API, runtime logs, and UI surfaces.

Purpose: satisfy SEC-02, SEC-03, and SEC-04 for credential handling hygiene.
Output: centralized redaction utilities, runtime/API integration, and regression tests.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-security-and-deployment-baseline/01-RESEARCH.md
@backend/app/core/security.py
@backend/app/runtime/agent_executor.py
@backend/app/api/websocket.py
@frontend/app/settings/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement centralized redaction/masking utilities</name>
  <files>backend/app/core/security.py</files>
  <action>Add reusable helpers to redact sensitive fields (e.g., `api_key`, `token`, `secret`, `password`, `authorization`) in nested dict/list/string payloads. Keep utility deterministic and reusable by API/runtime/log code paths.</action>
  <verify>cd backend && python -m compileall app && rg -n \"def .*redact|def .*mask\" app/core/security.py</verify>
  <done>Security module exposes tested redaction helpers that can sanitize nested payloads consistently.</done>
</task>

<task type="auto">
  <name>Task 2: Apply redaction before persistence and log broadcast</name>
  <files>backend/app/runtime/agent_executor.py, backend/app/api/websocket.py, backend/app/api/agents.py</files>
  <action>Wire redaction helpers into execution lifecycle so sensitive values are sanitized before writing `Execution.input_data`, `ExecutionStep.input_data/output_data`, and websocket/system log messages. Ensure `run_agent` request handling never leaks incoming credentials to persisted/logged payloads.</action>
  <verify>cd backend && rg -n "redact|mask" app/runtime/agent_executor.py app/api/websocket.py app/api/agents.py && python -m compileall app</verify>
  <done>No plaintext secret values are persisted or emitted through execution/system log paths.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce UI masking behavior and add redaction tests</name>
  <files>frontend/app/settings/page.tsx, backend/tests/test_secret_redaction.py</files>
  <action>Update settings UI flow so provider/API keys are treated as transient inputs (not persisted in localStorage in plaintext). Add backend tests that assert representative sensitive payloads are redacted in API/runtime paths and excluded from responses.</action>
  <verify>cd backend && pytest -q tests/test_secret_redaction.py && cd ../frontend && npm run lint -- --file app/settings/page.tsx</verify>
  <done>Credential UI remains masked and backend tests prove secret redaction across targeted surfaces.</done>
</task>

</tasks>

<verification>
- Integration and agent execution flows do not return/log plaintext credentials.
- API payload contracts used by frontend exclude secret values.
- Regression tests validate redaction behavior on nested payloads.
</verification>

<success_criteria>
SEC-02/SEC-03/SEC-04 are met by centralized redaction and automated checks that catch leakage regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-and-deployment-baseline/01-02-SUMMARY.md`
</output>
