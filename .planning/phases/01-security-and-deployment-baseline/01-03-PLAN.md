---
phase: 01-security-and-deployment-baseline
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - docker-compose.yml
  - compose.production.yaml
  - .env.example
  - docs/getting-started.md
  - README.md
autonomous: true
must_haves:
  truths:
    - "Operator can start a production-profile single-VM stack using Docker Compose with explicit production settings."
    - "Production deployment avoids development code bind mounts and has restart/health behavior suitable for uptime."
    - "Documented setup path reaches dashboard and backend health endpoints consistently."
  artifacts:
    - path: compose.production.yaml
      provides: "Production override configuration for secure single-VM deployment"
      min_lines: 35
    - path: docker-compose.yml
      provides: "Base compose config aligned with production overlay strategy"
      min_lines: 80
    - path: docs/getting-started.md
      provides: "Verified first-run production profile steps and checks"
      min_lines: 40
  key_links:
    - from: compose.production.yaml
      to: docker-compose.yml
      via: "docker compose multi-file merge for production"
      pattern: "docker compose -f docker-compose.yml -f compose.production.yaml"
    - from: compose.production.yaml
      to: backend/app/main.py
      via: "APP_ENV/APP_DEBUG/runtime env linking to startup guardrails"
      pattern: "APP_ENV|APP_DEBUG"
---

<objective>
Harden and verify single-VM Docker Compose deployment baseline.

Purpose: satisfy OPS-01 and provide reliable production-profile operability for this codebase.
Output: production compose overlay, env/docs alignment, and reproducible verification commands.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-security-and-deployment-baseline/01-RESEARCH.md
@docker-compose.yml
@.env.example
@docs/getting-started.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create production Compose overlay and tighten service runtime defaults</name>
  <files>compose.production.yaml, docker-compose.yml</files>
  <action>Add a production overlay file that removes dev code bind mounts, sets production-oriented env overrides, and applies restart policy/healthcheck tuning. Keep base compose developer-friendly while making production launch explicit via multi-file invocation.</action>
  <verify>docker compose -f docker-compose.yml -f compose.production.yaml config >/tmp/lazy-agents.compose.merged.yaml</verify>
  <done>Production profile is generated cleanly from compose merge and captures immutable-image/restart/health behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Align env contract with production security baseline</name>
  <files>.env.example, docs/getting-started.md</files>
  <action>Update environment template and setup docs to require production-safe values (`APP_ENV`, `APP_DEBUG`, strong `SECRET_KEY`, `API_KEY`) and document failure modes if missing. Keep instructions specific to single-VM operators.</action>
  <verify>rg -n "APP_ENV|APP_DEBUG|SECRET_KEY|API_KEY" .env.example docs/getting-started.md</verify>
  <done>Operators have explicit, accurate environment guidance for secure production startup.</done>
</task>

<task type="auto">
  <name>Task 3: Add reproducible baseline verification steps</name>
  <files>README.md, docs/getting-started.md</files>
  <action>Document command sequence to boot production profile and validate `/health`, `/health/ready`, and dashboard reachability. Include expected outputs and troubleshooting hints for common startup issues.</action>
  <verify>rg -n "compose.production.yaml|/health|/health/ready" README.md docs/getting-started.md</verify>
  <done>Docs include an end-to-end baseline verification flow that an operator can execute without ambiguity.</done>
</task>

</tasks>

<verification>
- Production compose config merges and validates successfully.
- Docs define exact secure env requirements and startup verification commands.
- Operator-facing setup flow reaches healthy API + dashboard endpoints.
</verification>

<success_criteria>
OPS-01 is satisfied with production-oriented Compose configuration and first-run verification instructions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-and-deployment-baseline/01-03-SUMMARY.md`
</output>
