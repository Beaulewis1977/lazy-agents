---
phase: 01-security-and-deployment-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/config.py
  - backend/app/core/security.py
  - backend/app/main.py
  - backend/tests/test_auth_fail_closed.py
  - backend/tests/test_startup_validation.py
autonomous: true
must_haves:
  truths:
    - "In non-development mode, protected API routes reject unauthenticated requests with 401."
    - "Application startup fails fast in non-development mode when required security settings are insecure or missing."
    - "Public health endpoints remain accessible while operational endpoints are protected."
  artifacts:
    - path: backend/app/core/config.py
      provides: "Typed production security guardrails and startup validation helpers"
      min_lines: 40
    - path: backend/app/core/security.py
      provides: "Fail-closed API key verification for non-development mode"
      min_lines: 40
    - path: backend/tests/test_auth_fail_closed.py
      provides: "Auth behavior regression tests across development/production modes"
      min_lines: 40
  key_links:
    - from: backend/app/core/config.py
      to: backend/app/main.py
      via: "startup validation invocation during app lifespan initialization"
      pattern: "validate|startup|lifespan"
    - from: backend/app/core/security.py
      to: backend/app/api/agents.py
      via: "router dependency enforcement with Depends(verify_api_key)"
      pattern: "APIRouter(dependencies=.*verify_api_key"
---

<objective>
Enforce fail-closed API authentication and startup validation for production profile.

Purpose: satisfy SEC-01 by making insecure production startup and unauthenticated protected access impossible.
Output: hardened config/security runtime path plus regression tests.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-security-and-deployment-baseline/01-RESEARCH.md
@backend/app/core/config.py
@backend/app/core/security.py
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add production startup security validation</name>
  <files>backend/app/core/config.py, backend/app/main.py</files>
  <action>Create explicit validation helpers that enforce non-development requirements for `API_KEY` and `SECRET_KEY` strength before app serves traffic. Call validation during lifespan startup so process exits early when config is insecure. Keep development behavior permissive for local iteration, but only when `APP_ENV=development`.</action>
  <verify>cd backend && python -m compileall app && rg -n "APP_ENV|API_KEY|SECRET_KEY|validate" app/core/config.py app/main.py</verify>
  <done>Production-mode startup path has a deterministic guardrail that blocks insecure config and emits actionable error details.</done>
</task>

<task type="auto">
  <name>Task 2: Make API auth fail-closed outside development</name>
  <files>backend/app/core/security.py, backend/app/api/agents.py, backend/app/api/skills.py, backend/app/api/integrations.py, backend/app/api/executions.py</files>
  <action>Update `verify_api_key` to reject missing credentials in non-development mode and preserve explicit 401 for missing/invalid keys. Keep health endpoints unauthenticated and verify protected routers remain guarded via dependency wiring. Avoid introducing route-by-route auth drift.</action>
  <verify>cd backend && rg -n "APIRouter\(dependencies=.*verify_api_key" app/api && python -m compileall app</verify>
  <done>All operational API routers consistently enforce auth in non-development mode; unauthenticated access is rejected.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for auth and startup behavior</name>
  <files>backend/tests/test_auth_fail_closed.py, backend/tests/test_startup_validation.py</files>
  <action>Add focused tests that cover: development permissive behavior, production missing key rejection, invalid key rejection, and startup guard failures for insecure defaults. Use FastAPI TestClient/async test patterns already compatible with current stack.</action>
  <verify>cd backend && pytest -q tests/test_auth_fail_closed.py tests/test_startup_validation.py</verify>
  <done>Tests fail if auth regresses to fail-open or if startup validation does not enforce non-development constraints.</done>
</task>

</tasks>

<verification>
- Non-development mode rejects unauthenticated access to `/api/agents`, `/api/skills`, `/api/integrations`, and `/api/executions`.
- Startup in production profile fails on insecure/missing key settings.
- Health endpoints remain reachable for orchestration health checks.
</verification>

<success_criteria>
SEC-01 is satisfied with automated regression coverage and explicit fail-closed behavior across protected routes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-and-deployment-baseline/01-01-SUMMARY.md`
</output>
