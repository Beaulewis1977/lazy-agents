---
phase: 02-mcp-server-control-plane
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - backend/app/mcp/__init__.py
  - backend/app/mcp/client.py
  - backend/app/mcp/manager.py
  - backend/app/api/mcp.py
  - backend/app/main.py
  - backend/tests/test_mcp_lifecycle.py
autonomous: true
must_haves:
  truths:
    - "Operator can restart and sync an MCP server through control-plane endpoints."
    - "Operator sees reliable MCP status transitions (`stopped`, `starting`, `running`, `error`) reflecting runtime reality."
    - "When MCP startup or sync fails, API surfaces actionable last-error details."
  artifacts:
    - path: backend/app/mcp/manager.py
      provides: "Lifecycle state machine and runtime registry for MCP servers"
      min_lines: 140
    - path: backend/app/mcp/client.py
      provides: "MCP SDK session helpers for initialize/list_tools/call patterns"
      min_lines: 70
    - path: backend/app/api/mcp.py
      provides: "restart/sync/status endpoints wired to lifecycle manager"
      min_lines: 90
  key_links:
    - from: backend/app/mcp/manager.py
      to: backend/app/models/mcp_server.py
      via: "status and last_error persistence on transition boundaries"
      pattern: "status\s*=|last_error\s*="
    - from: backend/app/api/mcp.py
      to: backend/app/mcp/manager.py
      via: "restart/sync endpoint dispatch"
      pattern: "restart|sync"
    - from: backend/app/main.py
      to: backend/app/mcp/manager.py
      via: "lifespan startup/shutdown manager initialization"
      pattern: "lifespan|app\.state\.mcp_manager"
---

<objective>
Build manager lifecycle state machine with restart/sync operations.

Purpose: satisfy MCP-03 and MCP-04 by making MCP runtime lifecycle observable and operable.
Output: lifecycle manager module, restart/sync/status API behavior, and lifecycle tests.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-mcp-server-control-plane/02-RESEARCH.md
@.planning/phases/02-mcp-server-control-plane/02-01-SUMMARY.md
@backend/app/main.py
@backend/app/runtime/agent_executor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement MCP client/session and lifecycle manager modules</name>
  <files>backend/app/mcp/__init__.py, backend/app/mcp/client.py, backend/app/mcp/manager.py</files>
  <action>Implement a manager that starts/stops/restarts MCP servers using MCP SDK stdio session helpers, tracks runtime handles by server ID, and enforces transition states (`stopped` -> `starting` -> `running`/`error`). Persist `status`, `last_error`, and discovered `tools_detected` updates through controlled manager methods.</action>
  <verify>cd backend && python -m compileall app && rg -n "class MCPServerManager|stdio_client|ClientSession|list_tools" app/mcp/*.py</verify>
  <done>Lifecycle manager can initialize sessions, sync tools, transition statuses, and capture actionable failure details.</done>
</task>

<task type="auto">
  <name>Task 2: Wire restart/sync/status API and FastAPI lifespan integration</name>
  <files>backend/app/api/mcp.py, backend/app/main.py</files>
  <action>Extend MCP API with `POST /api/mcp/servers/{id}/restart` and `POST /api/mcp/servers/{id}/sync`, and ensure list/get responses expose current status and last error text. Initialize manager in FastAPI lifespan (`app.state`) and shut down all managed runtimes during application teardown.</action>
  <verify>cd backend && python -m compileall app && rg -n "restart|sync|last_error|app\.state\.mcp_manager|lifespan" app/api/mcp.py app/main.py</verify>
  <done>Control-plane endpoints can trigger runtime operations, and lifecycle startup/shutdown behavior is centrally managed in app lifespan.</done>
</task>

<task type="auto">
  <name>Task 3: Add lifecycle regression tests for status transitions and failure surfacing</name>
  <files>backend/tests/test_mcp_lifecycle.py</files>
  <action>Add tests that simulate successful and failing lifecycle operations to verify transition ordering, restart/sync endpoint behavior, and persistence of `last_error` on failure. Include at least one test proving `running` -> `error` transition is visible to API consumers.</action>
  <verify>cd backend && pytest -q tests/test_mcp_lifecycle.py</verify>
  <done>Lifecycle behavior is test-guarded so status and actionable errors remain accurate across future changes.</done>
</task>

</tasks>

<verification>
- Restart and sync endpoints invoke manager operations and return updated server status.
- Lifecycle transitions are persisted and visible in list/get APIs.
- Failing runtime operations surface deterministic `last_error` details.
</verification>

<success_criteria>
MCP-03 and MCP-04 backend lifecycle behavior is implemented with deterministic status transitions and actionable failure visibility.
</success_criteria>

<output>
After completion, create `.planning/phases/02-mcp-server-control-plane/02-02-SUMMARY.md`
</output>
