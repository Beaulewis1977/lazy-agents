---
phase: 02-mcp-server-control-plane
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - frontend/lib/api.ts
  - frontend/components/DashboardLayout.tsx
  - frontend/app/integrations/mcp/page.tsx
  - frontend/app/integrations/page.tsx
autonomous: true
must_haves:
  truths:
    - "Operator can create, edit, and delete MCP server configurations from the UI with validation feedback."
    - "Operator can trigger restart and sync actions from the UI and see resulting status updates."
    - "When a server enters `error`, UI displays the latest actionable error details."
  artifacts:
    - path: frontend/lib/api.ts
      provides: "MCP API client contract and typed request/response helpers"
      min_lines: 80
    - path: frontend/app/integrations/mcp/page.tsx
      provides: "MCP management page with CRUD and lifecycle controls"
      min_lines: 180
    - path: frontend/components/DashboardLayout.tsx
      provides: "Navigation entry for MCP server control-plane workflow"
      min_lines: 10
  key_links:
    - from: frontend/app/integrations/mcp/page.tsx
      to: frontend/lib/api.ts
      via: "typed API calls for CRUD/restart/sync"
      pattern: "mcpServersAPI\.(list|create|update|remove|restart|sync)"
    - from: frontend/app/integrations/mcp/page.tsx
      to: /api/mcp/servers
      via: "status refresh after lifecycle actions"
      pattern: "load|refresh|status"
    - from: frontend/components/DashboardLayout.tsx
      to: frontend/app/integrations/mcp/page.tsx
      via: "sidebar navigation link"
      pattern: "href=\"/integrations/mcp\""
---

<objective>
Add MCP server management UI with status and error surfaces.

Purpose: complete operator-facing Phase 2 outcomes by exposing MCP CRUD + lifecycle controls in frontend.
Output: typed frontend API methods, MCP management page, and navigation wiring.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-mcp-server-control-plane/02-RESEARCH.md
@.planning/phases/02-mcp-server-control-plane/02-02-SUMMARY.md
@frontend/lib/api.ts
@frontend/app/integrations/page.tsx
@frontend/components/DashboardLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend frontend API client with MCP server endpoints and types</name>
  <files>frontend/lib/api.ts</files>
  <action>Add typed interfaces for MCP server records (including `status`, `last_error`, `tools_detected`) and expose API methods for list/create/get/update/remove/restart/sync. Keep request and response shapes aligned to backend contracts and existing `fetchAPI` error handling conventions.</action>
  <verify>cd frontend && npm run lint -- --file lib/api.ts</verify>
  <done>Frontend has a complete typed MCP API surface required by the management UI.</done>
</task>

<task type="auto">
  <name>Task 2: Build MCP management page for CRUD and lifecycle operations</name>
  <files>frontend/app/integrations/mcp/page.tsx, frontend/app/integrations/page.tsx</files>
  <action>Create a dedicated MCP management screen under `/integrations/mcp` that supports create/edit/delete forms for command/args/env/enabled and action buttons for restart/sync. Update the main integrations page with a clear entry point to MCP management while preserving existing integration behavior.</action>
  <verify>cd frontend && npm run lint -- --file app/integrations/mcp/page.tsx --file app/integrations/page.tsx</verify>
  <done>Operator can manage MCP server records and trigger lifecycle actions entirely from UI workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Surface runtime status transitions and actionable error details in UI navigation flow</name>
  <files>frontend/app/integrations/mcp/page.tsx, frontend/components/DashboardLayout.tsx</files>
  <action>Render visible status badges (`stopped`, `starting`, `running`, `error`) and last-error callouts with clear operator guidance. Add MCP navigation link in sidebar and refresh status after restart/sync actions so UI reflects backend transitions without stale state.</action>
  <verify>cd frontend && npm run lint -- --file app/integrations/mcp/page.tsx --file components/DashboardLayout.tsx</verify>
  <done>Status/error surfaces are visible, navigable, and updated after lifecycle actions, giving operators actionable control-plane feedback.</done>
</task>

</tasks>

<verification>
- MCP management page can perform CRUD and lifecycle actions against backend APIs.
- Status badges and last-error details are visible and update after restart/sync actions.
- MCP control-plane route is discoverable through standard dashboard navigation.
</verification>

<success_criteria>
Phase 2 operator-facing UI outcomes are met: MCP server configs and lifecycle state are manageable and observable from frontend.
</success_criteria>

<output>
After completion, create `.planning/phases/02-mcp-server-control-plane/02-03-SUMMARY.md`
</output>
