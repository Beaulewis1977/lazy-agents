---
phase: 02-mcp-server-control-plane
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/mcp_server.py
  - backend/app/models/__init__.py
  - backend/app/api/mcp.py
  - backend/app/main.py
  - backend/tests/test_mcp_api_crud.py
autonomous: true
must_haves:
  truths:
    - "Operator can create MCP server configurations with validated command, args, env, and enabled state."
    - "Operator can update and delete MCP server configurations through API endpoints."
    - "Sensitive MCP environment variable values are not exposed in standard API responses."
    - "Invalid MCP payloads are rejected with actionable validation errors instead of silent acceptance."
  artifacts:
    - path: backend/app/models/mcp_server.py
      provides: "Persisted MCP server configuration/status model"
      min_lines: 60
    - path: backend/app/api/mcp.py
      provides: "CRUD endpoints and request/response schemas for MCP servers"
      min_lines: 120
    - path: backend/tests/test_mcp_api_crud.py
      provides: "Regression tests for MCP CRUD and validation behavior"
      min_lines: 80
  key_links:
    - from: backend/app/api/mcp.py
      to: backend/app/models/mcp_server.py
      via: "SQLAlchemy CRUD operations with validated schema mapping"
      pattern: "select\(|MCPServer\(|await db\."
    - from: backend/app/api/mcp.py
      to: backend/app/core/security.py
      via: "credential encryption/masking helpers for env persistence and response shaping"
      pattern: "encrypt|decrypt|mask|redact"
    - from: backend/app/main.py
      to: backend/app/api/mcp.py
      via: "router registration under /api/mcp"
      pattern: "include_router\(mcp\.router"
---

<objective>
Implement MCP server data model and CRUD API endpoints.

Purpose: satisfy MCP-01 and MCP-02 by establishing persistent MCP server configuration management.
Output: MCPServer persistence model, validated CRUD endpoints, and regression tests.
</objective>

<execution_context>
@~/.codex/gsd/workflows/execute-plan.md
@~/.codex/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-mcp-server-control-plane/02-RESEARCH.md
@backend/app/models/integration.py
@backend/app/api/integrations.py
@backend/app/core/security.py
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persisted MCP server model and model registration</name>
  <files>backend/app/models/mcp_server.py, backend/app/models/__init__.py</files>
  <action>Create an `MCPServer` SQLAlchemy model with fields for name, description, command, args, env, enabled, status, tools_detected, last_error, created_at, and updated_at. Reuse existing JSON column conventions and register the model in package imports so table creation includes it.</action>
  <verify>cd backend && python -m compileall app && rg -n "class MCPServer|__tablename__ = \"mcp_servers\"" app/models/mcp_server.py</verify>
  <done>MCP server configuration/state is persistable with explicit status/error/tool cache fields and included in metadata registration.</done>
</task>

<task type="auto">
  <name>Task 2: Implement MCP CRUD API endpoints with strict validation</name>
  <files>backend/app/api/mcp.py, backend/app/main.py</files>
  <action>Add authenticated endpoints for list/create/get/update/delete MCP servers at `/api/mcp/servers`. Validate required fields (command, args type, env object shape) with Pydantic models, persist env values using existing security helpers for encrypted-at-rest storage, return normalized responses with masked env metadata (not plaintext secrets), and mount the router in `main.py`.</action>
  <verify>cd backend && python -m compileall app && rg -n "@router\.(get|post|put|delete).*mcp/servers|prefix=\"/api/mcp\"" app/api/mcp.py app/main.py</verify>
  <done>Operators can perform MCP server CRUD through typed endpoints with deterministic validation, proper HTTP status codes, and non-plaintext env handling in responses.</done>
</task>

<task type="auto">
  <name>Task 3: Add backend regression tests for MCP CRUD behavior</name>
  <files>backend/tests/test_mcp_api_crud.py</files>
  <action>Create focused API tests covering create/list/get/update/delete flows and validation failures for malformed payloads. Assert persisted status defaults and endpoint response contract stability to prevent regressions.</action>
  <verify>cd backend && pytest -q tests/test_mcp_api_crud.py</verify>
  <done>CRUD and validation behavior is protected by automated tests that fail on contract drift.</done>
</task>

</tasks>

<verification>
- `POST /api/mcp/servers` persists valid configuration and returns created entity.
- `PUT` and `DELETE` operations mutate the expected server and return correct status codes.
- Invalid payload shape and missing required fields return actionable 4xx responses.
</verification>

<success_criteria>
MCP-01 and MCP-02 API-level behavior is implemented and regression-tested, with stable model persistence and route wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/02-mcp-server-control-plane/02-01-SUMMARY.md`
</output>
